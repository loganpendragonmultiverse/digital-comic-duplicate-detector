# Synthetic test archive

Create two CBZ files from images you own or generate, then run:

```bash
comic-duplicate-detector first.cbz second.cbz --format json
```

The automated tests construct tiny synthetic ZIP archives in temporary directories; no copyrighted comic pages are distributed with this project.
